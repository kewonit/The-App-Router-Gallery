# Middleware

Run code before a request completes. Check authentication, redirect users, set headers, or rewrite URLs.

The `middleware.ts` file lives at the project root and runs on matched routes.

### Links

- [Docs](https://nextjs.org/docs/app/building-your-application/routing/middleware)
